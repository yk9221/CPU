#ifndef GLOBALS_H
#define GLOBALS_H

#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>

#include <fstream>

#define RAM_SIZE (64 * 1024)

using Byte = uint8_t;
using Word = uint16_t;

#endif
